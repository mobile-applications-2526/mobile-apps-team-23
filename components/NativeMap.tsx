import MapView, { Marker, Callout, PROVIDER_GOOGLE } from "react-native-maps";

// We export them as a single object or named exports
export { Marker, Callout, PROVIDER_GOOGLE };
export default MapView;
